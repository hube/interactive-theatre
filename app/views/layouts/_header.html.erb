<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("Interactive Theatre", root_path, class: 'navbar-brand') %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to('All Musicals', musicals_path) %></li>
        <li><%= link_to('Search Songs', songs_path) %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              My Account <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to("Add a New Musical", new_musical_path) %></li>
              <li><%= link_to("Add a New Song", new_song_path) %></li>
              <li><%= link_to("Sign Out", destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to("Sign In", new_user_session_path) %></li>
          <li><%= link_to("Register", new_user_registration_path) %></li>
        <% end %>
      </ul>
      <%= form_tag musicals_path, method: :get, class: "navbar-form navbar-right" do %>
        <div class="form-group">
          <%= text_field_tag :q, params[:q], placeholder: 'Search Musicals', class: 'form-control' %>
        </div>
        <%= submit_tag "Go!", class: 'btn btn-default' %>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
